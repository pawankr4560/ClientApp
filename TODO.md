# TODO
- [x] Fix bug: `exportSelectedAsExcel()` should export full installment schedule (per-month rows) instead of single due date
- [x] Verify TypeScript build passes (`ng build`)
- [x] Confirm Excel export outputs per-month installment rows (CSV)




